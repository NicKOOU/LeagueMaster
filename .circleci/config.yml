version: 2.1

orbs:
  node: circleci/node@4.7

jobs:
  eslint:
    executor: node/default
    steps:
      - checkout
      - node/install-packages
      - run:
          name: Run ESLint
          command: yarn run eslint

workflows:
  leagueMasterCI:
    jobs:
      - node/test:
          version: '16.10'
          pkg-manager: yarn
          filters:
            branches:
              only: BetterInJs
      - eslint:
          requires:
            - node/test