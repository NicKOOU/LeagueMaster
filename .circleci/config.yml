version: 2.1

orbs:
  node: circleci/node@4.7

jobs:
  eslint:
    executor: node/default
    steps:
      - checkout
      - node/install-packages
      - run:
          name: Run ESLint
          command: npm run eslint

workflows:
  leagueMasterCI:
    jobs:
      - node/test:
          version: '16.10'
          pkg-manager: npm
          filters:
            branches:
              only: BetterInJs
      - eslint:
          pkg-manager: npm
          requires:
            - node/test